{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm config set prefix /usr/local && npm install -g pnpm@latest && pnpm install && pnpm --filter=blushbite-web run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter=blushbite-web run start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
